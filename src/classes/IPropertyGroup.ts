/**
 * Defines meta-data for every visual editor property group (category)
 *
 * @export
 * @interface IPropertyGroup
 */
export default interface IPropertyGroup {
  name: string;
  displayName: string;
  orderno: number;
}
